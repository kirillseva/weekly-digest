div(ng-include='"components/navbar/navbar.html"')



.container
  h1.controls
    i(class="fa fa-pause" ng-show="playing" ng-click="pauseTrack()")
    i(class="fa fa-play" ng-hide="playing" ng-click="playTrack()")
    p
    i(class="fa fa-forward" ng-class="{disabled: currentSkips===0}" ng-click="skipTrack()")
  img(src="{{albumArt}}", alt="{{currentMix.name}}")
  p.lead {{name}}
  .progress(id="progress" style="width: 0%")


//- {{EightTracks.resp}}
footer.footer
  .container
    p
      | Kirill Sevastyanenko
      = ' | '
      | 2014

audio(id="player", type="audio/mpeg")
